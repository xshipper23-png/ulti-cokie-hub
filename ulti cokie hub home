local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

local Window = Rayfield:CreateWindow({
   Name = "ulti cokie hub",
   Icon = 0, -- Icon in Topbar. Can use Lucide Icons (string) or Roblox Image (number). 0 to use no icon (default).
   LoadingTitle = "pls wait",
   LoadingSubtitle = "by mr cokie",
   ShowText = "ulti cokie hub", -- for mobile users to unhide rayfield, change if you'd like
   Theme = "Dark Blue", -- Check https://docs.sirius.menu/rayfield/configuration/themes

   ToggleUIKeybind = "g", -- The keybind to toggle the UI visibility (string like "K" or Enum.KeyCode)

   DisableRayfieldPrompts = true,
   DisableBuildWarnings = false, -- Prevents Rayfield from warning when the script has a version mismatch with the interface

   ConfigurationSaving = {
      Enabled = false,
      FolderName = nil, -- Create a custom folder for your hub/game
      FileName = "Hub"
   },

   Discord = {
      Enabled = true, -- Prompt the user to join your Discord server if their executor supports it
      Invite = "https://discord.gg/2N28DkBv", -- The Discord invite code, do not include discord.gg/. E.g. discord.gg/ ABCD would be ABCD
      RememberJoins = false -- Set this to false to make them join the discord every time they load it up
   },

   KeySystem = true, -- Set this to true to use our key system
   KeySettings = {
      Title = "pls enter key",
      Subtitle = "anti bot system",
      Note = "get keys from my discord", -- Use this to tell the user how to get a key
      FileName = "c-hub-k", -- It is recommended to use something unique as other scripts using Rayfield may overwrite your key file
      SaveKey = false, -- The user's key will be saved, but if you change the key, they will be unable to use your script
      GrabKeyFromSite = false, -- If this is true, set Key below to the RAW site you would like Rayfield to get the key from
      Key = {"0404"} -- List of keys that will be accepted by the system, can be RAW file links (pastebin, github etc) or simple strings ("hello","key22")
   }
})

Rayfield:Notify({
   Title = "ulti cokie",
   Content = "thanks for using ulti cokie",
   Duration = 7,
   Image = nil,
})

local keyTab = Window:CreateTab("start game", nil) -- Title, Image
local gamesTab = Window:CreateTab("game", nil) -- Title, Image

local Section = gamesTab:CreateSection("possebil things")

local Label = gamesTab:CreateLabel("🔴= out of order", nil, Color3.fromRGB(255, 255, 255), false) -- Title, Icon, Color, IgnoreTheme

local Label = gamesTab:CreateLabel("🟢= working", nil, Color3.fromRGB(255, 255, 255), false) -- Title, Icon, Color, IgnoreTheme

local Label = gamesTab:CreateLabel("🟡= not workig works 50%", nil, Color3.fromRGB(255, 255, 255), false) -- Title, Icon, Color, IgnoreTheme

local Label = gamesTab:CreateLabel("⚫= comming soon", nil, Color3.fromRGB(255, 255, 255), false) -- Title, Icon, Color, IgnoreTheme

local Label = gamesTab:CreateLabel("🔵= updating today", nil, Color3.fromRGB(255, 255, 255), false) -- Title, Icon, Color, IgnoreTheme

local Label = gamesTab:CreateLabel("⭐= new update!!", nil, Color3.fromRGB(255, 255, 255), false) -- Title, Icon, Color, IgnoreTheme

local Section = gamesTab:CreateSection("GAMES")

local Label = gamesTab:CreateLabel("🟢⭐prison life", nil, Color3.fromRGB(255, 255, 255), false) -- Title, Icon, Color, IgnoreTheme
local Label = gamesTab:CreateLabel("⚫build a boat", nil, Color3.fromRGB(255, 255, 255), false) -- Title, Icon, Color, IgnoreTheme
local Label = gamesTab:CreateLabel("⚫99 nights in the forest", nil, Color3.fromRGB(255, 255, 255), false) -- Title, Icon, Color, IgnoreTheme

local Button = keyTab:CreateButton({
   Name = "prisone life",
   Callback = function()
   -- The function that takes place when the button is pressed
   end,
})
